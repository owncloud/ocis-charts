apiVersion: v1
kind: Service
metadata:
  name: experimental
  namespace: {{ template "ocis.namespace" . }}
  labels:
    {{- include "ocis.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: 9180
      protocol: TCP
  selector:
    app: experimental
