apiVersion: v1
kind: Service
metadata:
  name: app-provider
  namespace: {{ template "ocis.namespace" . }}
  labels:
    {{- include "ocis.labels" . | nindent 4 }}
spec:
  ports:
    - name: grpc
      port: 9164
      protocol: TCP
  selector:
    app: app-provider
