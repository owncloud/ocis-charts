apiVersion: v1
kind: Service
metadata:
  name: graph-explorer
  namespace: {{ template "ocis.namespace" . }}
  labels:
    {{- include "ocis.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: 9135
      protocol: TCP
  selector:
    app: graph-explorer
