apiVersion: v1
kind: Service
metadata:
  name: ocdav
  namespace: {{ template "ocis.namespace" . }}
  labels:
    {{- include "ocis.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
  selector:
    app: ocdav
