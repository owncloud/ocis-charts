apiVersion: v1
kind: Service
metadata:
  name: thumbnails
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "ocis.labels" . | nindent 4 }}
spec:
  ports:
  - name: grpc
    port: 9185
    protocol: TCP
  selector:
    app: thumbnails
